# 5분야 전수 평가 + 다음 계획 — 260729c

라이브 `20260729-172` · 미배포 0 · vitest 24,114 통과(실패 0) · tsc 0

앞 문서(`next-session-plan-260729b.md`)의 P0 4건 · P1 3건 · P2 3건과 코퍼스 「남은 것」
3건까지 **전부 완료**했다. 이 문서는 그 뒤 다시 돌린 **50종 전수 실측**이다.

---

## 1. 실측 (50종 · 1,626부품 · 게이트 FAIL 0 · 마크업 누출 0)

| 분야 | 템플릿 | 부품 | 실판정 있음 | **평균 실판정** | 자기정합 | 입력 대기 | 미판정 고지 | FAIL |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 조경 | 8 | 242 | 2 | **6.0** | 0 | 2 | 0 | 1 |
| 인테리어 | 9 | 250 | 9 | **5.1** | 0 | 25 | 0 | 0 |
| 교량 | 6 | 464 | 6 | **4.3** | 4 | 0 | 3 | 3 |
| 건축 | 8 | 245 | 7 | **3.9** | 0 | 1 | 0 | 1 |
| 기계 | 16 | 415 | 9 | **2.9** | 0 | 0 | 10 | 2 |
| 토목(civil) | 3 | 10 | (별도 경로) | — | 0 | 0 | 0 | 0 |

### ⚠ 이전 평가의 인테리어 「14~16」은 **과대평가였다**

260729b 에서 인테리어 깊이를 14~16 으로 적었는데, 그 중 상당수가 실제로는 **입력 대기**
였다 — `verdict:'INPUT(...)'` 가 "판정: INPUT(footing)" 으로 찍혀 **판정으로 세어졌다.**
P0-4 에서 「입력 대기 — 판정하지 않음」으로 분리한 뒤 실판정은 **5.1**, 입력 대기가 25건이다.

→ **판정 개수는 표시 마커를 신뢰할 수 있을 때만 셀 수 있다.** 260729b 는 「표시 계층으로
능력을 재지 말 것」을 교훈으로 적었는데, 정작 그 표시가 **판정과 입력 대기를 섞고 있어서**
반대 방향(과대)으로도 틀렸다. 같은 자를 두 번 다르게 틀린 셈이다.

### 세션 간 변화 (같은 기준으로 다시 잰 값)

| 분야 | 260729b | 260729c | 원인 |
|---|---:|---:|---|
| 기계 | 2.0 | **2.9** | 정적 전도 배선 |
| 교량 | 2~4 | **4.3** | 압축재 좌굴(KDS 14 31 25) |
| 건축 | 3.7 | 3.9 | 설비 관통 |
| 인테리어 | (14~16) | 5.1 | ← 측정 정정, 실제 저하 아님 |

템플릿 49 → **50**(변단면 연속 거더교 신설).

### FAIL 7건 — 성격이 둘로 나뉜다

**실제 설계 신호 5건** (전부 원인 규명 완료):
- `building/commercial_massing` — 이중골조 요건 25% 미달(골조 분담 0.5%)
- `landscape/timber_deck` — 목재 부재
- `bridge/arch_bridge` — 행어 ratio 1.387(중실 모델 자중 인공물, 실단면 선언 시 PASS)
- `mech/four_bar`·`mech/robot_arm` — 정적 전도(CG 가 지지 밖: y 68.4mm · x 162.5mm 초과)

**입력 의존 2건** — `bridge/girder_bridge`·`tapered_girder_bridge`.
평가용으로 **내가 임의로 준 배근 As=8,000mm²** 로는 부족하다는 뜻이지 템플릿 결함이 아니다.
배근을 안 주면 「입력 대기」다. ⚠ **이 구분을 안 하면 FAIL 수를 품질 지표로 오독한다.**

### 정당하게 실판정 0 인 13종 (사유 개별 확인)

- `building/steel_canopy` — 지붕 골조형이라 하중경로 적용범위 밖
- 조경 6종 — 목재 부재가 없다(검토 대상 아님)
- 기계 6종 — 설계압력·카운터웨이트·굴착력·전선장력 등 지어낼 수 없는 지배 입력 요구

---

## 2. 이번 실측이 새로 찾은 것

### ⚠ A. 기계 6종에서 **전도가 문서에 한 글자도 안 나온다** (P0)

```
tower_crane: mechCheck.checks = { staticTipover, seismicTipover }   ← 계산은 된다
             안전검토.html    = "검토 불가: 카운터웨이트…"           ← 전도 없음
             verdict.unavailable = ["입력 필요: 카운터웨이트…"]      ← 전도 없음
```

`domainSafetyReportHtml` 의 `r.ok === false` 분기가 **checks 를 통째로 버리고** "검토 불가"
카드만 그린다. P1-5 에서 배선한 정적 전도가 **정작 그것이 유일한 판정인 6종에서** 사라진다.

이 세션에서 반복해 잡은 「조기 반환이 고지를 삼킨다」의 **네 번째** 사례다(공간 고지 ·
`lateralUnavailable` · `basis` 조건 · 그리고 이번). 같은 형태가 계속 나온다는 것은
**패턴을 고쳐야 한다**는 뜻이다 — 개별 자리를 막는 것으로는 끝나지 않는다.

### ⚠ B. `cafe_room` 에 위생기구·배관이 하나도 없다 (P1)

부품 39개짜리 카페인데 `fixtures 0 · pipes 0` 이라 배수·통기 검사가 「대상 없음」으로 빠진다.
`two_room` 도 같다. **검사 결함이 아니라 템플릿 데이터 갭**이다 — 실제 카페는 싱크·화장실이
있고, 그것이 인테리어 검사의 핵심 입력이다. 어휘도 검사도 이미 있는데 **쓸 데이터가 없다.**

### ⚠ C. 새 어휘·검사를 쓰는 출하 템플릿이 없다 (P2)

- `slab_with_openings` 를 쓰는 **건축 템플릿 0개** (관통 검사가 늘 「관통 없음」)
- 벽 관통 대조도 **합성 테스트에서만** 재현된다
- `tapered_girder` 는 260729b 에 템플릿을 만들어 해소했다 — 같은 처방이 필요하다

어휘를 만들고 검증까지 해도 **출하 경로가 없으면 실사용에서 무엇이 깨지는지 알 수 없다.**

---

## 3. 다음에 할 일 — **전항 완료 (260729c 후반)**

| # | 항목 | 결과 |
|---|---|---|
| P0-1 | `ok:false` 의 checks 를 버리지 않기 | 완료 — 기계 6종에 전도가 도달. 렌더 **진입점 한 곳**에서 처리 |
| P0-2 | verdict 에 실판정 결과도 | 완료 — `judgedDespiteRefusal`·`judgedNote` |
| P1-3 | `cafe_room` 위생기구·배관 | 완료 — 기구 3·배관 4, 배수·통기가 실제로 돈다. ⚠ `two_room` 은 **정당하게 제외** |
| P1-4 | 기계 간섭이 판정에 닿는가 | **이미 배선돼 있었다**(오진 방지 확인 — 아래) |
| P1-5 | 조경 고유 검사 | 완료 — 제원 자기정합 3종(실판정 템플릿 2 → 5) |
| P2-6 | `slab_with_openings` 건축 실사용 | 완료 — `commercial_massing` 계단실·PS 개구 + 입상관 |
| P2-7 | 벽 관통 템플릿 | **미착수 — 억지로 만들지 않았다**(아래) |
| 확인-8 | 토목 두 문서 | 정상 — 검증.html 2,721자 생성 + 쉬운요약 도달 확인 |

### ⚠ P1-4 는 결함이 아니었다 — 내가 오진할 뻔했다

`buildAssembly` 를 직접 호출해 `transmission_tower` 간섭 **186건**을 보고 "정상 설계가
오류로 나간다"고 판단하려 했다. 실제로는 **패키지 경로에 2차 정제가 이미 배선돼 있다**:

```
정제 전 186 → 정제 후 75 (해제 111 · 과탐 59.7%)
profile: { allLatticePairs: true, maxIntersectMm3: 36234 }
```

`refineInterferencesMesh` + `applyInterferenceRefinement` 가 route.ts:350 에서 돈다.
**중간 함수만 호출해 놓고 파이프라인 전체의 결함이라 결론지을 뻔했다** — 이 세션에서
반복한 「측정 지점을 잘못 잡으면 없는 결함을 만든다」의 또 다른 형태다.

### P2-7 을 하지 않은 이유

슬래브 관통은 출하 경로가 생겼고(`commercial_massing`) 벽 관통 로직은 회귀로 검증됐다.
벽 관통 실사용을 만들려면 `duct_run` 에 없는 벽을 세우거나 덕트 경로를 비틀어야 하는데,
**템플릿을 부자연스럽게 바꾸면서까지 검사 커버리지를 채우는 것**은 실무 형상을 왜곡한다.
다음에 벽을 지나는 설비가 자연스러운 템플릿이 생기면 그때 붙인다.

### 이 과정에서 잡은 것

- **입상관 시작 z 오류** — `tz:200` 으로 뒀다가 기초 슬래브(0~250)를 50mm 파고들어
  「기초에 개구가 없다」오탐이 났다. **방금 만든 관통 검사가 바로 잡았다** — 검사가
  자기 템플릿의 결함을 잡은 첫 사례다.
- **회귀 2건이 깨졌고 둘 다 새 상태가 맞았다**: 조경 3종이 「판정 0개」에서 빠졌고
  (제원으로 판정하므로), `commercial_massing` 이 「설비 없음」에서 빠졌다(입상관이 생겼으므로).
  근거를 테스트에 남기고 갱신했다.

---

## 4. 측정 방법 메모 (두 번 틀린 자를 고친 기록)

| 시점 | 잰 방법 | 결과 | 무엇이 틀렸나 |
|---|---|---|---|
| 260729b | HTML `판정:` 개수 | 교량 1.0 | 렌더러가 `ok` 를 안 그려 **과소** |
| 260729b | 〃 | 인테리어 14~16 | `INPUT` 이 판정으로 찍혀 **과대** |
| 260729c | `판정:` − `안전 판정 아님`, `입력 대기` 별도 | 교량 4.3 · 인테리어 5.1 | 표시를 고친 뒤라야 셀 수 있다 |

**표시 계층으로 능력을 재려면 표시가 먼저 정확해야 한다.** 그 전에는 어느 방향으로
틀렸는지도 알 수 없다 — 같은 자로 재고도 한 번은 과소, 한 번은 과대였다.
