# A–E reference re-audit — 2026-08-05

## Sources rechecked

- `document(manuals)`: all 20 indexed PDFs exist and match the recorded byte counts and SHA-256 hashes.
- `참고파일들`: the governed `result/inventory.json` and `result/ir_index.json` remain the source of local corpus discovery; CAD bytes remain read-only and are not copied into this repository.
- `7.3 example/CAD_통합_백과사전_공학이론_수학과학_공식_계산체계_최종판.md`: coordinate, tolerance, assembly, B-rep healing, BIM/interior, clash and validation sections were re-routed to the implementation map below.

## Applied now

| Reference principle                                                                                     | Implementation                                                                                                     |
| ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| World/model/local coordinates and occurrence transforms must remain distinct                            | `stepAssemblyEvidence.ts`, `productSpatialIr.ts`, `ifcSpatialStructure.ts`                                         |
| Compound is not a Solid; spatial containers are not physical parts                                      | exact OCCT body-boundary assertions and Product/Spatial IR validation                                              |
| Import recovery must preserve the original and report repaired vs originally valid                      | `stepRecoveryPipeline.ts`, `cadHealingPolicy.ts`                                                                   |
| Healing must re-check validity, solids, volume change, topology and tolerance growth                    | `cadHealingPolicy.ts`                                                                                              |
| Sewing tolerance must remain below the smallest governed feature/edge                                   | `cadHealingPolicy.ts`                                                                                              |
| Static mechanical, spatial and MEP evidence must share a fail-closed release gate                       | `crossDomainVerification.ts` and CAD v1 project verification                                                       |
| Missing IFC placement must not become identity                                                          | `ifcSpatialStructure.ts`                                                                                           |
| Straight IFC grid intersections must restore occurrence coordinates through their complete parent chain | exact `IfcGridPlacement` subset and row-major world matrix in `ifcSpatialStructure.ts`                             |
| Door/robot/hinge motion must not tunnel through thin obstacles between sampled frames                   | analytic point-arc/segment intersection in `clearanceFanChecker.ts`; `arcSamples` is compatibility-only            |
| Unknown units and sampled/partial evidence must remain explicit                                         | corpus verdicts retain `not_run`, byte-budget and unsupported-entity reasons rather than manufacturing coordinates |

## Still partial — do not certify as complete

- OCCT `ShapeFix_Shape`/`BRepBuilderAPI_Sewing` now execute through the Node OCCT bridge with exact before/after validity, solid count, volume, tolerance and minimum-edge measurements. It is still bounded recovery rather than proof that every damaged vendor STEP can be repaired.
- Continuous collision detection includes exact linear swept-AABB interval overlap and adaptive rotational interval subdivision, while also forcing every authored keyframe into the checked intervals. Rotational endpoint bounds tighten by `radius * sin(angle/2)` at each subdivision; proven-clear intervals are discarded, midpoint AABB hits are sent to FeatureTree mesh/OCCT refinement, and budget-exhausted intervals remain release-blocking `unresolved`. Exact analytic rotating B-rep-vs-B-rep time-of-impact remains partial.
- Door swing/activity clearance geometry: door-edge point arcs and numeric egress/grid travel checks exist, but finite-thickness 3D door swept volumes still need a governed geometry adapter.
- IFC exact world matrices cover local placement chains, the governed straight-axis grid subset, and LinearPlacement with an explicit CartesianPosition. A fail-closed source-vs-reimport semantic comparator now verifies GlobalId, spatial parents, property/quantity/material definitions and ProjectedCRS/MapConversion; curve-evaluated LinearPlacement without CartesianPosition and automatic rich-semantic export remain partial.
- Shared geodetic coordinate approval and large-coordinate origin-shift restoration remain separate civil/BIM work.
- Healing now explicitly invalidates pre-healing subshape references and requires the existing conservative geometric remapper/ambiguity review before reuse. Complete history-based naming across every external healing and feature-recognition operation remains partial.

## Fail/not_run policy

- Reduce `not_run` only by adding an executable evidence adapter or authoritative project input.
- Never convert absence (`0→0`, missing transform, missing collision geometry) into preservation or clearance evidence.
- Keep `HEALING_EXCEEDED_TOLERANCE`, continuous-motion gaps, unresolved IFC placement and missing code inputs release-blocking.
- Re-run local corpora after each adapter promotion and record assertion-level deltas rather than only fixture totals.

## Final reference pass additions

- Re-read the 20-file manuals inventory, the governed `inventory.json`/`ir_index.json` corpus notes, and the encyclopedia coordinate, tolerance, assembly, collision, IFC and healing sections after implementation.
- Preserved the corpus rules that unknown units stay unknown, sampled evidence stays labelled, and unsupported binary/vendor formats do not enter the AI generation fleet as invented geometry.
- The reference corpus warns that curvature ratio alone cannot prove parametric recoverability; future mesh-to-feature promotion must also require pattern, primitive residual, planar-cluster and triangle-count evidence.
- The next highest-value executable closures are the single remaining railing profile/thickness input, richer alignment curve evaluation, structural unit-normalized load validation, and exact rotating B-rep collision time-of-impact.

## Actual IFC corpus result

- 87 local IFC files were analyzed read-only with path/source suppression.
- Historical v3 baseline: after strengthening the verdict to require every physical occurrence to produce geometry, the audited baseline was 80 pass / 7 not_run. It is superseded by domain/recovery v5 below; the earlier 68/19 figure is not directly comparable because it allowed representative/partial geometry imports to pass.
- `fail=0` and runner `error=0`; valid-but-unsupported IFC remains `not_run`.
- Historical v3 gap: two specialized fixtures plus 161 zero-thickness occurrences. V5 closes both specialized fixtures and 63 quantity-backed doors; 98 wall/railing occurrences remain. Both former 128 MiB exclusions execute under the governed 300 MB limit.

## Final D/E integration evidence

- The browser generation session records the ordered `intent -> decomposition -> interfaces -> part_programs -> kernel -> topology -> assembly_solve -> motion -> manufacturing -> roundtrip -> release` state with SHA-256 checkpoints and minimal edit invalidation.
- `generation/advance` derives kernel, watertight topology and assembly-certificate evidence from the applied multi-part FeatureTrees. `generation/finalize` server-verifies required animation frames, evaluates per-part G0-G7, compares STEP export/re-import measurements for G8, and keeps exact-artifact G9 authorization explicit.
- Web, CAD v1 API, CLI (`generation advance|finalize`) and MCP (`advance_ai_generation`, `finalize_ai_generation`) use the same contracts. None creates a quote or RFQ.
- Final focused integration regression: 1,944 passed / 3 skipped, with no failed tests. Manual trace: 20 manuals / 16 baseline requirements valid. Route-security, TypeScript and guarded real STEP roundtrip smoke all passed.
- Final read-only STEP corpus rerun wrote a signed summary for 25 fixtures: 25 pass / 0 fail / 0 not_run / 0 error, 425 assertions, 78/78 restored occurrence world placements. Its signature remains `fff713360043e75b0f55d0679acafc7b00fa02215e47f893894ae2b8048290f6`.
- Historical semantic v3 read-only rerun was 80 pass / 7 not_run / 0 fail / 0 error; retain it only as the before-state for the v5 comparison.
- The semantic v3 rerun (`ifc-spatial-baseline-260805-semantic-v3-summary.json`) preserves that 80/7 verdict while measuring 30,284 occurrence identities, 68,420 property/quantity/material definitions and 44 projected-georeference entities. It found zero duplicate encoded IFC GlobalIds. An initial five-file false alarm was traced to ordinary classification URIs/property names in argument zero and fixed by applying uniqueness only to the IFC 22-character encoded GlobalId domain.
- The same semantic comparator is now exposed consistently as `POST /api/cad/v1/ifc/semantic-roundtrip`, CLI `ifc semantic-roundtrip`, and MCP `verify_ifc_semantic_roundtrip`; all accept inline IFC pairs, return no source body, and create no quote/RFQ.
- Domain/recovery v5 improves the actual 87-file verdict from 80 pass / 7 not_run to 83 pass / 4 not_run, with zero fail/error. Sixty-three planar doors recover their missing depth only from a unique positive `IfcElementQuantity` volume/area ratio; conflicting ratios remain blocked. The remaining 98 occurrences (97 walls, one railing) contain no governed authoritative thickness and still request input.
- The former two specialized-domain `not_run` cases now produce explicit IR instead of fabricated solids: the alignment sample preserves 2 horizontal, 2 vertical and 2 cant design segments plus projected georeferencing; the structural sample preserves 4 nodes, 3 fully connected curve members, 1 activity, 9 reactions and 19 load entities. This is exposed as `POST /api/cad/v1/ifc/domain-ir`, CLI `ifc domain-ir`, and MCP `build_ifc_domain_ir`.
- Domain/recovery v6 further improves the same 87-file corpus to 86 pass / 1 not_run / 0 fail / 0 error. Ninety-seven apparent zero-thickness walls were proven to be `IfcRelAggregates` parents whose physical wall-part children were already imported; Axis/FootPrint on an aggregate parent is now retained as reference data but never duplicated as a solid. The sole remaining blocker is one railing with measured bounds 0 × 1754.4194 × 1000 mm and no profile, physical width, material layer, or type representation.
- Recovery-contract v7 preserves 86/1/0/0 after a unit-regression audit. It emits the remaining railing as an occurrence-scoped request (`GlobalId 3jNbvLAhL0YvMN0lzMe4bX`, missing axis 0, required `profile_definition_or_physical_width_mm`) through API `POST /api/cad/v1/ifc/recovery-plan`, CLI `ifc recovery-plan`, and MCP `plan_ifc_geometry_recovery`. The first project length SI unit is now locked so later conversion-only SI units cannot rescale coordinates.
- Authoritative-recovery execution now closes that request loop through API `POST /api/cad/v1/ifc/recover-geometry`, CLI `ifc recover-geometry --file ... --inputs ...`, and MCP `recover_ifc_geometry`. Inputs are bound to an exact IFC GlobalId, require one and only one missing axis, a positive width, and explicit provenance; duplicate IDs, complete-geometry overwrite attempts, and unused inputs fail closed. The actual corpus file was re-run with zero supplied inputs and correctly remains `releaseReady=false` at entity `#119350`; the 50 mm passing value is confined to a synthetic contract fixture and is not represented as corpus truth. Evidence: `ifc-authoritative-recovery-260805-summary.json`.
- Specialized-validation v8 keeps 86/1/0/0 while validating four alignment files (20 horizontal, 16 vertical and 12 cant segments; every vertical/cant station chain continuous) and one structural-analysis file. All 14 numeric load records normalize successfully through IFC conversion/derived units; measured factors include 4.44822162 N/lbf, 0.0254 m/in, 175.12683543307088 N/m per source linear-force unit and 0.112984829148 N·m per source moment unit. Unknown load units now block the domain IR instead of passing raw values.
- The STEP baseline CLI now explicitly terminates after its atomic summary is flushed because the headless OCCT/Emscripten runtime may retain internal handles; imported test calls are unaffected.
- Animation CCD regression now proves that the former full-radius rotational envelope false positive clears after adaptive subdivision, and that an insufficient subdivision budget returns `verified=false` rather than silently passing. CAD v1 API, CLI `animation verify`, and MCP `verify_assembly_animation` share this verdict; MCP exposes governed `rotationalMaxDepth` 0-16. Focused regression: 9/9 tests plus TypeScript passed.
- A subsequent interval-coverage audit closed a narrow-phase safety hole: clearing one midpoint mesh candidate no longer clears the unsampled time on either side. The rotational recursion continues until every child interval is proven clear or retained as `ROTATIONAL_CCD_UNRESOLVED`; absent FeatureTree/OCCT collision geometry is separately reported as `COLLISION_GEOMETRY_MISSING`. Both map to bounded `collision-refine` recovery and remain release-blocking. The browser timeline now displays continuous candidates and unresolved rotational intervals instead of claiming sampled-only clearance. Focused integration regression: 22/22 tests, MCP syntax, and TypeScript passed.
- `collision-refine` is now executable rather than advisory: the shared verifier retries only rotational subdivision at deterministic depths (default 6, 8), stops immediately when interval coverage is proven, and records depth plus unresolved-before/after/resolved counts for every attempt. Remaining leaves then enter the precise mesh-distance stage rather than multiplying to depth 10. Exhaustion remains `verified=false`; identical geometry is never modified and clearance is never invented. Browser verification and CAD v1 API use the same runner, so CLI/MCP responses include the same `ccdRecovery` evidence.
- Precise rotational leaves now use actual FeatureTree/OCCT tessellated surfaces. The verifier measures triangle-mesh minimum separation at the interval midpoint and compares it with a conservative bound on both parts' possible translation plus rotational chord displacement over the entire leaf. Only `separation > motionBound + tolerance` becomes `proven_clear`; a real mesh intersection becomes `confirmed_collision`, and insufficient separation remains unresolved. Work is capped at 256 precise leaves per request; overflow is `PRECISE_CCD_BUDGET_EXCEEDED`, never pass. An integration fixture with continuously overlapping AABBs but disjoint L-channel/peg solids now releases from executed mesh-distance evidence.
- Linear CCD now makes the same broad/narrow distinction: swept-AABB overlap is exact for boxes but only a candidate for arbitrary concave solids. Every linear candidate enters midpoint mesh separation versus the full interval translation bound. A disjoint peg moving inside an L-channel AABB is now `proven_clear`, while an actual between-frame pass-through is `confirmed_collision`; insufficient proof is `LINEAR_CCD_UNRESOLVED`. API, CLI, MCP and capability discovery expose the shared result.
- IFC horizontal alignment IR now evaluates start distance, source start point/direction/radii, end point and end direction. LINE/CIRCULARARC use analytic formulas; CLOTHOID/COSINE/SINE/BLOSS/HELMERT use governed curvature profiles with deterministic 256/512-step RK4 error evidence. CUBIC now uses the IFC common line-to-arc cubic with numerical arc-length inversion and rejects a non-line endpoint. VIENNESEBEND runs only when a station/length-matched cant segment, source `RailHeadDistance`, and provenance-bearing gravity-center height are all present; otherwise the IR returns the exact missing-input list. Project `PLANEANGLEUNIT` is normalized to radians. Scale-aware junction checks report measured position/direction errors. The actual v9 corpus remains 87 files, 86 pass / 1 not_run / 0 fail / 0 error: it contains no CUBIC/VIENNESE horizontal occurrence, so those new paths are synthetic governed regressions rather than a corpus claim.
- Structural-load coverage is now exhaustive for IFC4 static numeric leaf types: single/linear/planar force, displacement/distortion, force warping, and temperature. Unit factors are taken from `IfcUnitAssignment` or dimensionally derived from the assigned force/length units; any future/unknown `IFCSTRUCTURALLOAD*` numeric entity is retained in `unsupportedLoadEntities` and blocks release with `structural_load_type_unsupported` instead of disappearing. Actual `structural-curve-member.ifc` evidence remains valid with 4 nodes, 3 members, 14/14 numeric loads normalized, 0 unit-unresolved, and 0 unsupported (`ifc-structural-load-coverage-260805-summary.json`).
- Continuous assembly verification now reports a conservative earliest collision-time bracket. It searches candidate time intervals from left to right; each earlier interval must be proven clear by executed FeatureTree/OCCT mesh separation exceeding its rigid-body motion bound. Depth, frame tolerance, and evaluation count are governed, and exhaustion emits release-blocking `PRECISE_TOI_UNRESOLVED`. The Web assembly timeline has an explicit precise-verification action, displays the pair/bracket, and can jump to its first-possible frame; API, CLI, and MCP preserve the same controls/evidence. A tunnelling fixture narrowed frames 0–10 to `[2.449951171875, 2.45025634765625]` in 26 evaluations after 9 clear interval proofs (`assembly-continuous-toi-260805-summary.json`).
- IFC `IfcLinearPlacement` now falls back from absent CartesianPosition to `IfcAxis2PlacementLinear -> IfcPointByDistanceExpression`, evaluating distance-along plus lateral/vertical/longitudinal offsets against the governed horizontal and vertical alignment. Constant/parabolic gradients and vertical circular arcs restore elevation and the tangent frame; unsupported station geometry remains unresolved. A new shared `ifc spatial-ir` API/CLI/MCP operation returns occurrence world transforms and cross-check evidence. Actual v10 corpus remains 86/1/0/0 and independently recomputes all 28 explicit linear placements: 28/28 cross-checked, zero mismatches, maximum position difference 1.5934771423859253e-7 source units and basis difference 6.391614668088685e-13.
- STEP mechanical relations now extract each `CYLINDRICAL_SURFACE` axis from its local placement and transform it by the owning part occurrence before comparing distinct parts. `concentric_mate` and named shaft-bearing layout use governed angular/linear tolerances; sufficient measured axes with no match fail, while genuinely insufficient analytic geometry remains `not_run`. API `step/mechanical-relations`, CLI `reference mechanical-relations`, and MCP `analyze_step_mechanical_relations` share the same evidence. The actual `NutBolt.STEP` fixture measured 2 solids, 53 axes and 492 coaxial surface pairs; after separating geometry-only STEP roundtrip from PMI preservation, the fastener scenario changed from 0 pass / 1 not_run to 1 pass / 0 not_run (`step-mechanical-relations-260805-summary.json`).
- STEP pattern fidelity now groups repeated child definitions and verifies their authoritative world origins as equal-pitch linear or circular arrangements. Three non-collinear points remain `not_run` because they trivially define a circle; four or more are required for circular proof. Measured irregular spacing is `fail`, never `not_run`. The actual `DIFFERENTIAL GEAR BOX FILE.STEP` contains two six-occurrence circular groups, each with 60-degree pitch and maximum deviations of `4.046762924758696e-13` and `3.8669067947694207e-13` source units. `pattern_fidelity` is now pass; the overall gearbox scenario honestly remains `not_run` because the source is one assembly level deep and contains no named shaft/bearing roles (`step-pattern-fidelity-260805-summary.json`).
- STEP manufacturing evidence now distinguishes what imported geometry actually proves. The actual `A0101.STEP` cabinet source has 463 occurrences and two distinct `3 x 48 x 762.5` constant-thickness panel occurrences, so `independent_panels` passes; exact flat topology and bend history remain `not_run`. The corpus selector no longer stops at one 455 MB over-budget candidate and safely selects the next ranked 22.5 MB STEP source. The actual `SPIRAL STAIRCASE.stp` source has 9 measured solids, from which two elongated members and two deterministic profile/length cut-list groups are proven. Source solid entity IDs are preserved into the bridge, and the exact STEP end-plane equations are intersected with each member centreline; both members now have resolved cut lengths and end angles, so `miter_lengths` passes. Density-based mass remains `not_run` because the source does not authoritatively bind material density. Shared STEP mechanical-relations API, CLI and MCP expose the same sheet/weldment evidence (`step-sheet-weldment-evidence-260805-summary.json`).

- Sheet-metal recovery performs a budgeted second pass only for measured panel candidates instead of classifying all 463 occurrences. A bend is accepted only from a coaxial inner/outer cylindrical-surface pair whose radius delta equals the measured thickness and whose axis lies in the sheet plane. `A0101.STEP` has no such pair, so it remains correctly classified as two flat panels rather than receiving invented bends. The importer retains analytic circular inner-loop centres/radii and emits machine-readable remediation codes. An intermediate run correctly reported `NAUO126/128:outer_arcs_approximated`; the following analytic-curve change resolved those codes without tolerance widening.

- Analytic curve retention now composes `ORIENTED_EDGE` orientation with `EDGE_CURVE.same_sense` and preserves circle/ellipse centre, basis, radii, start angle, and signed sweep. Exact Green-theorem area correction replaces chord-only area, while B-spline approximations remain fail-closed. Re-running `A0101.STEP` promotes both `flat_pattern` assertions to `pass`. Because both complete parts are exact planar extrudes with no in-plane bend-surface pair, `bend_table` is an authoritative empty table and also passes. The electrical-cabinet scenario is now `pass` (1 pass, 0 fail, 0 not_run) with all five assertions passing.
