# 상세 어휘 확장 + 5분야 평가 — 260801b

라이브 `20260801-180` → 이번 작업 `20260801-181` · 템플릿 54종 · 어휘 37종

「부품·어휘를 추가해 더 디테일한 것까지 잡고 복합적으로 구현 가능하게」를 실행했다.
**어휘를 늘리는 것보다 「이미 있는 상세를 다른 어휘가 못 쓰는 것」이 먼저였다** —
그것을 코퍼스 실측으로 확정하고 시작했다.

---

## 1. 코퍼스가 지목한 상세 갭 4개 (실측)

| 갭 | 코퍼스 근거 | 착수 전 상태 |
|---|---|---|
① 홀 가공 상세 | 홀 보유 **683파일** | `holeFeature`(관통·블라인드·카운터보어·싱크·탭)가 **완전히 구현돼 있는데 `plate_with_holes` 만 썼다** |
② 패턴 | **248파일**(원형 455·선형 175) · 패턴 소속 홀 **1,796개** | 패턴 어휘 **0** — 홀을 하나씩 열거해야 했다 |
③ 필렛 | **157파일** · 반경 표본 **2,095개**(중앙값 1.43mm · 0~150mm) | 필렛 파라미터를 가진 어휘 **0** |
④ 복합(중첩) | 형상 aspect `complex` **447/1071 = 42%** | `cavity_block` 하나뿐(금형 전용) |

①의 실증 — 같은 카운터보어를 주면:
```
plate_with_holes  → 475,024 mm³   (cbore 24×8 을 실제로 공제)
extrude_profile   → 477,738 mm³   (through 와 동일 = cbore 를 무시)
```
`extrude_profile` 은 **실물 압출의 67.6%**를 커버하는 어휘인데 홀이 단순 원통뿐이었다.
코드는 이미 있고 **배선만 없었다** — 이 세션 내내 잡아 온 형태 ①이다.

---

## 2. 한 것

### ① 홀 상세를 단일 소스로
`holeVolume`·`holeWallArea`·`holeScad` 를 뽑아 `plate_with_holes` 와 `extrude_profile` 이
**같은 식**을 쓴다. 어휘마다 다시 구현하면 언젠가 갈린다 — 실제로 갈려 있었다.
BOQ 홀 수도 **패턴 전개 기준**으로 센다(선언 1건이 실제 가공 6회일 수 있다).

### ② 패턴 — 정보를 잃지 않는다
`pattern:{kind:'linear'|'circular', count, pitch|bcd, angleDeg?|startDeg?}` → 구체 홀로 전개.
⚠ 전개된 홀에 **`_pat`(패턴 출처)를 남긴다.** 좌표만 남기면 도면에 `4-M12 EQ.S.` 로
나가야 할 것이 좌표 4개로 나가고 BOQ 는 가공 회차를 알 수 없다.
⚠ 모르는 `kind` 는 **펼치지 않고 게이트가 이름으로 거부한다** — 선형으로 가정하면
사용자가 준 뜻과 다른 형상이 나간다.

### ③ 필렛 — 형상에 **실제로** 반영
기존 스키마의 `FilletFeature` 는 「record-only(OpenSCAD 에 필렛 엔진이 없다)」였다.
그런데 **압출 프로파일의 필렛은 2D 문제**다 — 볼록 꼭짓점을 원호(현 분할)로 바꾸면
부피·표면적·SCAD·STEP·GA 가 **전부 같은 형상**을 본다.
부피만 보정하고 형상은 그대로 두는 길은 **택하지 않았다**(둘이 어긋난다).

정사각 r10 실측: 면적 9,912.1 vs 정확값 9,914.16 → 오차 **2.02mm²(0.02%)**, 새그 0.048mm.
⚠ **볼록만** 라운드한다(오목은 재료가 늘어나는 쪽이라 별건).
⚠ 반경이 인접 변 절반을 넘으면 **줄이지 않고 거부**한다 — 조용히 줄이면 사용자가 준
반경과 다른 형상이 나간다.

### ④ 복합 부품 — `composite`
`subs:[{type, params, at?, op?}]` 의 합·차. 손검산 대조: 1,149,588mm³ 일치.
⚠ 중첩은 **1단만**(하위의 하위 금지) — 열어 두면 부피·AABB·간섭·STEP 이 어디까지
정확한지 아무도 말할 수 없다.
⚠ add 끼리 **겹침은 공제하지 않는다**(과대 가능) — 어휘 힌트에 적었다.
⚠ **표면적은 미산출(null)** — 접촉면을 알 수 없다. BOQ 가
「표면적 미산출: composite — 도장 물량 합계에 **빠져 있다**(0이 아니라 모름)」로 고지한다.
⚠ 하위는 **자기 게이트**로 검사한다(여기서 다시 구현하면 규칙이 갈린다).

### ⑤ 새 템플릿에 판정 붙이기
- `boltedPlateCheck` — 볼트 **연단거리**(가공연 1.5d / 압연연 1.25d **두 기준 대조** —
  가공법을 우리가 정하지 않는다)·**볼트 간격**(3d 관례)·**여유 과대**.
  ⚠ `composite` **안쪽 판재**도 본다 — 최상위만 보면 `motor_mount` 가 통째로 빠진다.
- `masonryCheck` — **세장비**(무보강 20 / 보강 30 **두 기준 대조**)·**줄눈 두께**(8~13mm)·
  **인방 지지길이**·블록 규격 정합. 조적은 전단벽이 아니므로 **메타 디스패치**를 뒀다.

### 새 템플릿 `mech/motor_mount`
상세 어휘 4종을 **한 부품에서 동시에** 쓴다: 복합(베이스+웨브+리브 2) · 카운터보어 앵커홀 ·
모터 볼트 **원형 패턴** · 모서리 **필렛**. 어휘를 늘리고 소비자를 안 만드는 실수를
이 세션에 두 번 했기 때문에, 이번엔 소비자를 같이 만들었다.

---

## 3. ★ 검사가 **내 템플릿의 결함 3건**을 즉시 잡았다

| 결함 | 어떻게 드러났나 |
|---|---|
`gusset_bracket` **같은 자리에 홀 두 개** | 「볼트 간격 최소 **0mm**」 — 두 변을 각각 `edge` 에서 시작해 모서리가 중복됐다 |
`gusset_bracket`·`motor_mount` **연단거리 0.9배** | 카운터보어 머리(⌀26)가 자리를 먹는 것을 안 셌고, 거셋은 **사선 변이 지배**하는데 축 방향으로만 밀고 있었다(`edge` 40→55 로 늘려도 계속 미달) |
**빌더 폴백 ≠ 템플릿 default** | `edgeDist` 템플릿 55 vs 폴백 40 — `buildAssemblyTemplate(d,id,{})` 는 default 를 **채우지 않는다** |

세 번째는 난간 `handrailH`(템플릿 1,200 vs 폴백 1,000)로 **이미 겪은 부류**다.
두 번 겪고서야 전수로 막았다 — 아래 회귀.

### 복합 AABB 가 회전을 무시하고 있었다
`motor_mount` 의 웨브(rx=90)가 **회전 전 경계**로 계산돼 261×172×26 이 나왔다(실제 200×140×174).
AABB 는 **간섭·GA·지지 판정이 모두 쓰는 값**이라 회전을 빼먹으면 그 전부가 틀린다.
8코너 회전으로 고쳤다(`placedAabb` 와 같은 규약).
+ 리브 회전도 틀렸다 — `gusset` 로컬축은 x=legA·y=legB·z=두께라 두께를 X 로 세우려면
순환 치환 `(x,y,z)→(z,x,y)` = `rx:90, rz:90` 이 필요하다. `rx:90` 만 주면 폭이 200→261 이 된다.

### 내가 만든 안티패턴을 내가 잡았다
`bolted-plate-check.mjs` 에 `expandHoles` 를 **복제**해 두고 「규칙이 갈리면 위험하다」는
주석까지 달았다. 그게 바로 이 세션 내내 고쳐 온 안티패턴이라 단일 소스 import 로 바꿨다.
복제하면 **판정이 형상과 다른 홀을 보게 된다.**

---

## 4. 새로 박은 회귀 — 두 층의 구멍을 막았다

| 회귀 | 막는 것 |
|---|---|
`PARAMS ⇄ CANONICAL` 양방향 | 새 어휘가 전수 소비자에서 빠지는 것(260801 에 겪음) |
전 어휘 게이트·부피·AABB 양수 | 인벤토리에 넣어 놓고 게이트를 못 통과하는 것 |
**빌더 폴백 = 템플릿 default** (전수) | 경로에 따라 형상이 달라지는 것. ⚠ 파생·조건부 기본값 2건은 **이유와 함께 허용목록**(`bendR`=od×1.5 · `rollerPitch`=형식별)이고, 「이제 일치하는 예외는 남기지 않는다」까지 검사 |
**템플릿이 판정을 갖는가** | 실판정 0이면 **정당한 이유를 선언**해야 통과(civil 3종=검증 경로 · `flanged_fitting`=부속류) |
상세 어휘 22건 | 어휘 간 홀 부피 일치 · 패턴 전개·정보 보존 · 모르는 kind 거부 · 필렛 면적·새그·볼록만 · 과대 반경 거부 · 복합 폐형·AABB·표면적 미산출 고지·하위 게이트 |

---

## 5. 실측 (54종 · 입력 충실)

| 분야 | 템플릿 | 실판정 | 미판정 | FAIL |
|---|---:|---:|---:|---:|
| 인테리어 | 9 | 50 | 6 | 0 |
| 기계 | 18 | **47** (43→) | 6 | 6 |
| 건축 | 10 | **33** (31→) | 18 | 1 |
| 조경 | 8 | 28 | 1 | 1 |
| 교량 | 6 | 23 | 2 | 8 |
| 토목 | 3 | (검증 경로) | 0 | 0 |
| **합계** | **54** | **181** (175→) | 33 | 16 |

영문 라벨 **0** · 소비자 도달 **53/54** · 게이트 self-test **49/54**(기존 5건 그대로)

---

## 6. 다음에 할 일

### P0 — 임포터의 다음 층
1. **`CYLINDRICAL_SURFACE + PLANE` 22건 = 구멍 뚫린 판.** 원호 근사 후 미지원 사유 1위다.
   우리는 이제 `extrude_profile` + 원형 홀로 **정확히 그 형상을 표현할 수 있다** —
   캡 평면에서 외곽을, 원통면에서 홀(중심·반경)을 얻어 조합하면 된다.
   ⚠ 원통 축이 압출 축과 **평행할 때만** 홀이다. 비평행 원통은 홀이 아니라 다른 형상이므로
   평행 판정을 먼저 하고, 아니면 종전대로 거부해야 한다.

### P1 — 상세의 남은 층
2. **오목 필렛(라운드 대신 살 붙임)** — 지금은 볼록만 한다. 오목은 재료가 늘어나는 쪽이라
   부피가 커지는데, 어느 쪽인지 모른 채 반영하면 물량이 틀린다. 규약을 먼저 정할 것.
3. **복합 표면적** — 접촉면을 알아야 한다. `subs` 의 AABB 접촉으로 근사할 수 있으나
   ⚠ 근사값을 미산출 대신 넣으면 「모름」이 「값」으로 바뀐다 — 근사임을 수치로 고지해야 한다.
4. **패턴을 도면 표기에 연결** — `_pat` 를 남겨 뒀지만 GA·부품도가 아직 쓰지 않는다.
   `4-M12 EQ.S.` 로 나가야 정보가 소비자에 닿는다(지금은 좌표로만 나간다).

### P2 — 남은 미판정 33건
5. 성격은 대부분 규명돼 있다: `(참고)` 8 · 건축 적용범위/편심/미관통 13 ·
   인테리어 배수 4(하강 경로 불성립) · 기계 전도 2 · 교량 좌굴 2 · 난간 살 1.
   **움직일 수 있는 것은 난간 살 배치**(별도 설계 작업)뿐이다.
