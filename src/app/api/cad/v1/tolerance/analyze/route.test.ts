import { describe, expect, it } from 'vitest'; import { NextRequest } from 'next/server'; import { POST } from './route';
it('computes worst-case and RSS and uses worst-case for release', async () => { const p = await (await POST(new NextRequest('http://localhost/api/cad/v1/tolerance/analyze', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ dimensions: [{ id: 'a', name: 'a', nominal: 10, tolerancePlus: .1, toleranceMinus: -.1, direction: 1, distribution: 'normal' }, { id: 'b', name: 'b', nominal: 5, tolerancePlus: .1, toleranceMinus: -.1, direction: -1, distribution: 'normal' }], lowerSpec: 4.7, upperSpec: 5.3 }) }))).json(); expect(p.result.nominal).toBe(5); expect(p.result.worstCaseMax).toBeCloseTo(5.2); expect(p.designOk).toBe(true); });
