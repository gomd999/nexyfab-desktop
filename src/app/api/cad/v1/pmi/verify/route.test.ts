import { expect, it } from 'vitest'; import { NextRequest } from 'next/server'; import { POST } from './route';
it('moves unresolved topology references to review', async () => { const p = await (await POST(new NextRequest('http://localhost/api/cad/v1/pmi/verify', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ callouts: [{ id: 'g', viewportId: 'v', kind: 'flatness', targetRef: 'missing', toleranceValue: .1 }], validTopologyRefs: ['face.top'] }) }))).json(); expect(p.verified).toHaveLength(0); expect(p.review).toHaveLength(1); expect(p.designOk).toBe(false); });
