import{NextRequest}from'next/server';import{describe,expect,it}from'vitest';import{POST}from'./route';
const req=(body:unknown)=>new NextRequest('http://localhost/api/cad/v1/ifc/spatial-ir',{method:'POST',headers:{'content-type':'application/json','x-forwarded-for':'ifc-spatial-ir-test'},body:JSON.stringify(body)});
describe('IFC spatial IR route',()=>{it('returns distance-evaluated linear placement without echoing source',async()=>{const ifc=`ISO-10303-21;HEADER;FILE_SCHEMA(('IFC4X3_ADD2'));ENDSEC;DATA;#1=IFCALIGNMENT('alignment',$,'A',$,$,$,$,$);#2=IFCCARTESIANPOINT((0.,0.));#3=IFCALIGNMENTHORIZONTALSEGMENT($,$,#2,0.,0.,0.,100.,$,.LINE.);#20=IFCPOINTBYDISTANCEEXPRESSION(IFCLENGTHMEASURE(10.),2.,3.,$,#99);#21=IFCAXIS2PLACEMENTLINEAR(#20,$,$);#22=IFCLINEARPLACEMENT($,#21,$);#23=IFCSIGNAL('signal',$,'Signal',$,$,#22,$,$);ENDSEC;END-ISO-10303-21;`,json=await(await POST(req({ifc}))).json();expect(json).toMatchObject({ok:true,releaseReady:true,sourceReturned:false,ir:{linearPlacementEvidence:{total:1,distanceEvaluated:1},nodes:[{worldTransform:expect.any(Array)}]}});expect(JSON.stringify(json)).not.toContain('ISO-10303-21');});it('rejects path fields',async()=>expect((await POST(req({ifc:'x',path:'C:\\x.ifc'}))).status).toBe(400));});
