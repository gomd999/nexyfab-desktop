/**
 * eng-domain/landscape — public surface for landscape-design gate-material checks.
 * Pure, deterministic checks (관수·배수·식재간격·조경면적·객토) consumed by the
 * landscape DomainModule (Batch 3). See ./checks for the honesty contract & sources.
 */
export * from './checks';
