import { describe, expect, it } from 'vitest'; import { buildRadianceDaylightPackage, daylightInvalidations } from '../radianceDaylightPackage';
describe('Radiance daylight package',()=>{it('builds deterministic metric scene and sensor artifacts with weather provenance',()=>{const input={projectId:'p',materials:[{id:'wall',type:'plastic' as const,rgb:[.5,.5,.5] as [number,number,number]}],surfaces:[{id:'s',materialId:'wall',verticesMm:[[0,0,0],[1000,0,0],[0,1000,0]] as [number,number,number][]}],sensors:[{id:'q',positionMm:[500,500,800] as [number,number,number],direction:[0,0,1] as [number,number,number]}],weatherFileName:'site.epw',weatherFileSha256:'a'.repeat(64),northRotationDeg:15};const a=buildRadianceDaylightPackage(input),b=buildRadianceDaylightPackage(input);expect(a.manifest).toEqual(b.manifest);expect(a.sceneRad).toContain('1 0 0');expect(a.manifest.sensorCount).toBe(1)});it('invalidates only affected daylight evidence',()=>{expect(daylightInvalidations(['name_only'])).toEqual([]);expect(daylightInvalidations(['window_geometry'])).toEqual(expect.arrayContaining(['direct_sun_access','annual_daylight']));expect(daylightInvalidations(['furniture'])).toEqual(['interior_daylight_obstruction'])})});
