import { describe, expect, it } from 'vitest'; import { verifyWeldedEnclosureFamilyContractBytes } from './weldedEnclosureContract'; import { buildWeldedEnclosureFamilyContractFixture } from './weldedEnclosureContract.testFixture'; const enc = (value: unknown) => new TextEncoder().encode(JSON.stringify(value));
describe('welded/enclosure family contract', () => { it('recomputes member, weld, bend, datum, door, grounding and ingress design gates', () => { const f = buildWeldedEnclosureFamilyContractFixture(), report = verifyWeldedEnclosureFamilyContractBytes(f.graphBytes, f.contractBytes, f.artifacts); expect(report).toMatchObject({ status: 'passed', familyContractReady: true, calculated: { minimumMemberSafetyFactor: 2, minimumWeldSafetyFactor: 4, maximumDatumErrorMm: 0.3, maximumGroundResistanceOhm: 0.05, minimumDoorHingeSafetyFactor: 2 }, checks: { systemGraph: true, componentRoles: true, members: true, welds: true, sheetBends: true, distortion: true, datums: true, doors: true, grounding: true, coating: true, ingressDesign: true, evidence: true }, weldInspectionComplete: false, dimensionalInspectionComplete: false, ingressCertificationClaimed: false, releaseReady: false }); }); it('fails insufficient weld area and missing bend relief', () => { const f = buildWeldedEnclosureFamilyContractFixture(); f.contract.welds[0]!.throatMm = 0.1; f.contract.sheets[0]!.bendReliefProvided = false; const report = verifyWeldedEnclosureFamilyContractBytes(f.graphBytes, enc(f.contract), f.artifacts); expect(report.errors).toEqual(expect.arrayContaining(['welded_enclosure_check_failed:welds', 'welded_enclosure_check_failed:sheetBends'])); }); it('does not convert an IP design plan into certification or release', () => { const f = buildWeldedEnclosureFamilyContractFixture(); f.contract.ingress.designRating = 'IP55'; const report = verifyWeldedEnclosureFamilyContractBytes(f.graphBytes, enc(f.contract), f.artifacts); expect(report).toMatchObject({ familyContractReady: false, ingressCertificationClaimed: false, releaseReady: false }); }); });
