import { generateKeyPairSync, sign } from 'node:crypto'; import { describe, expect, it } from 'vitest'; import { nativeCadSignoffPayload, type NativeCadExpertSignoff, type TrustedReviewerKeys } from '@/lib/reference/nativeCadExpertReview'; import { verifyRobotFinalReleaseReview } from './robotFinalReleaseReview';
const enc = (v: unknown) => new TextEncoder().encode(`${JSON.stringify(v)}\n`), targetHash = 'a'.repeat(64), audit = { schema: 'nexyfab.robot-release-evidence-audit.v1', status: 'ready_for_final_review', releaseTargetHash: targetHash, releaseReady: false, errors: [], blockers: ['final_release_dual_signoff_required'] }; const a = generateKeyPairSync('ed25519'), b = generateKeyPairSync('ed25519'); const trusted: TrustedReviewerKeys = { alice: { publicKey: a.publicKey.export({ type: 'spki', format: 'pem' }).toString(), roles: ['domain-reviewer'] }, bob: { publicKey: b.publicKey.export({ type: 'spki', format: 'pem' }).toString(), roles: ['independent-reviewer'] } };
function signed(role: NativeCadExpertSignoff['role'], id: string, key: typeof a.privateKey, target = targetHash) { const unsigned = { role, reviewerId: id, decision: 'approved' as const, reviewedAt: '2026-08-09T00:00:00.000Z', targetHash: target }; return { ...unsigned, signature: sign(null, Buffer.from(nativeCadSignoffPayload(unsigned)), key).toString('base64') }; } const review = () => ({ schema: 'nexyfab.robot-final-release-review.v1' as const, targetHash, signoffs: [signed('domain-reviewer', 'alice', a.privateKey), signed('independent-reviewer', 'bob', b.privateKey)] });
describe('robot final release review', () => { it('marks exact fully evidenced dual approval release-ready without executing release', () => { expect(verifyRobotFinalReleaseReview(enc(audit), review(), trusted, Date.parse('2026-08-09T00:01:00Z'))).toMatchObject({ approved: true, releaseReady: true, releaseExecuted: false, errors: [], blockers: [], sideEffects: { persisted: false, cadModified: false, releasePublished: false, quoteCreated: false, rfqSent: false } }); }); it('rejects another target, one reviewer, and non-ready evidence', () => { const wrong = review(); wrong.signoffs[0] = signed('domain-reviewer', 'alice', a.privateKey, 'b'.repeat(64)); expect(verifyRobotFinalReleaseReview(enc(audit), wrong, trusted, Date.parse('2026-08-09T00:01:00Z')).approved).toBe(false); const one = review(); one.signoffs.pop(); expect(verifyRobotFinalReleaseReview(enc(audit), one, trusted, Date.parse('2026-08-09T00:01:00Z')).releaseReady).toBe(false); expect(verifyRobotFinalReleaseReview(enc({ ...audit, status: 'not_ready', releaseTargetHash: null }), review(), trusted, Date.parse('2026-08-09T00:01:00Z')).approved).toBe(false); }); });
