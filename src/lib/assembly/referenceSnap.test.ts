import{describe,expect,it}from'vitest';import{snapAssemblyReferences}from'./referenceSnap';import{IDENTITY_QUAT,type AssemblyState}from'./assemblyState';import type{GeometryResolver}from'./iterativeSolver';const state:AssemblyState={parts:[{id:'fixed',name:'fixed',partTemplateId:'x',position:{x:0,y:0,z:0},orientation:IDENTITY_QUAT,fixed:true},{id:'move',name:'move',partTemplateId:'x',position:{x:10,y:0,z:0},orientation:IDENTITY_QUAT}],mates:[]};const resolve:GeometryResolver=(ref,part)=>ref.refKind==='point'?{kind:'point',world:part.position}:ref.refKind==='axis'?{kind:'axis',world:{origin:part.position,direction:ref.partId==='move'?{x:1,y:0,z:0}:{x:0,y:0,z:1}}}:{kind:'plane',world:{origin:part.position,normal:{x:0,y:0,z:1}}};describe('reference snap',()=>{it('snaps points and suggests coincident',()=>{const r=snapAssemblyReferences(state,{partId:'move',refId:'origin',refKind:'point'},{partId:'fixed',refId:'origin',refKind:'point'},resolve);expect(r.state.parts[1]!.position).toEqual({x:0,y:0,z:0});expect(r.suggestedMate.kind).toBe('coincident');});it('aligns axes and suggests concentric',()=>{const r=snapAssemblyReferences(state,{partId:'move',refId:'x',refKind:'axis'},{partId:'fixed',refId:'z',refKind:'axis'},resolve);expect(r.rotationDeg).toBeCloseTo(90);expect(r.suggestedMate.kind).toBe('concentric');});});
