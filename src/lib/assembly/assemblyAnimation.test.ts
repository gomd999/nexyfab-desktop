import {describe,expect,it} from 'vitest'; import {evaluateAssemblyAnimation,validateAssemblyAnimation,type AssemblyAnimation} from './assemblyAnimation'; import {IDENTITY_QUAT,type AssemblyState} from './assemblyState';
const state:AssemblyState={parts:[{id:'base',name:'Base',partTemplateId:'base',position:{x:0,y:0,z:0},orientation:IDENTITY_QUAT,fixed:true},{id:'arm',name:'Arm',partTemplateId:'arm',position:{x:0,y:0,z:0},orientation:IDENTITY_QUAT}],mates:[]};
const animation:AssemblyAnimation={version:1,name:'move',fps:30,startFrame:0,endFrame:100,tracks:[{id:'arm-pose',targetPartId:'arm',keyframes:[{frame:0,position:{x:0,y:0,z:0}},{frame:100,position:{x:100,y:0,z:0},orientation:{x:0,y:0,z:1,w:0},interpolation:'smoothstep'}]}]};
describe('assembly animation IR',()=>{it('evaluates a multi-part pose without flattening bodies',()=>{const out=evaluateAssemblyAnimation(state,animation,50);expect(out.parts).toHaveLength(2);expect(out.parts[1]!.position.x).toBeCloseTo(50);expect(Math.hypot(...Object.values(out.parts[1]!.orientation))).toBeCloseTo(1);});it('rejects unknown parts and duplicate frames',()=>{const bad={...animation,tracks:[{id:'x',targetPartId:'ghost',keyframes:[{frame:0,position:{x:0,y:0,z:0}},{frame:0,position:{x:1,y:0,z:0}}]}]};expect(validateAssemblyAnimation(bad,state).join(' ')).toMatch(/unknown part.*duplicate frame/);});});
