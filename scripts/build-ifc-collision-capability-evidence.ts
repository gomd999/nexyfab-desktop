import fs from 'node:fs';
import path from 'node:path';
const input = path.resolve(process.argv[2] ?? 'docs/evidence/external-step-structure-coverage-260806/ifc-fidelity-run-1.json');
const output = path.resolve(process.argv[3] ?? 'docs/evidence/external-step-structure-coverage-260806/ifc-collision-capability-run-1.json');
const fidelity = JSON.parse(fs.readFileSync(input, 'utf8')) as { summary: { totals: { imported: number; exactDisplayMesh: number; exactMeshVolume: number; openSurfaceMesh: number } } };
const totals = fidelity.summary.totals, artifact = { schema: 'nexyfab.ifc-collision-capability.v1', generatedAt: new Date().toISOString(), scoreEligible: false, releaseReady: false, policy: { triangleSurfaceIntersectionCanConfirmCollision: true, openMeshCannotClearContainment: true, pairBudgetExhaustionIsNotRun: true, coplanarInPlaneSeparatingAxesRequired: true }, summary: { importedMeshes: totals.imported, surfaceIntersectionEligible: totals.exactDisplayMesh, closedMeshCompleteEligible: totals.exactMeshVolume, confirmationOnlyOpenMeshes: totals.openSurfaceMesh, surfaceEligibilityCoverage: totals.exactDisplayMesh / totals.imported, completeCollisionCoverage: totals.exactMeshVolume / totals.imported }, gates: { collisionConfirmation: totals.exactDisplayMesh === totals.imported ? 'pass' : 'fail', completeCollisionAndClearance: totals.exactMeshVolume === totals.imported ? 'pass' : 'not_run' } };
fs.mkdirSync(path.dirname(output), { recursive: true }); fs.writeFileSync(output, `${JSON.stringify(artifact, null, 2)}\n`, 'utf8'); console.log(JSON.stringify({ output: path.relative(process.cwd(), output), ...artifact.summary, releaseReady: artifact.releaseReady }));
