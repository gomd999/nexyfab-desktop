ARG CAD_RUNTIME_BASE_IMAGE
FROM ${CAD_RUNTIME_BASE_IMAGE}

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev --ignore-scripts && npm cache clean --force
COPY server.mjs ./server.mjs

USER node
CMD ["node", "server.mjs"]
