import { describe, expect, it } from 'vitest';
import { hmacCommercialTransport, validateCommercialExecutionJob, validateCommercialTransport, verifyCommercialTransportHmac, type CommercialTransportEnvelope } from './commercialPrecisionExecution';
const job = { contractVersion: 'nexyfab.precision-cad-commercial-execution.v2' as const, jobId: 'job-1', tenantId: 'tenant-1', projectId: 'project-1', executionId: 'exec-1', generationRunId: 'run-1', generationStateRevision: 11, generationProgramSha256: '9'.repeat(64), workspaceId: 'workspace-1', workspaceRevision: 7, workspaceContentHash: 'a'.repeat(64), tool: 'build_assembly', scope: 'apply' as const, callId: 'call-1', argumentsHash: 'b'.repeat(64), commandHash: 'c'.repeat(64), targetHash: 'd'.repeat(64), journalVersion: 3, attempt: 1, leaseGeneration: 1 };
describe('commercial precision execution contract v2', () => {
  it('binds deterministic job identity and HMAC transport', async () => { expect(validateCommercialExecutionJob(job)).toEqual([]); const unsigned = { schema: 'nexyfab.precision-cad-commercial-execution.v2' as const, job, issuedAt: '2026-08-22T00:00:00.000Z', expiresAt: '2026-08-22T00:04:00.000Z', callbackUrl: 'https://worker.example.test/callback', leaseCapability: 'x'.repeat(43) }; const envelope: CommercialTransportEnvelope = { ...unsigned, transportHmac: await hmacCommercialTransport('s'.repeat(32), unsigned) }; expect(validateCommercialTransport(envelope)).toEqual([]); expect(await verifyCommercialTransportHmac('s'.repeat(32), envelope)).toBe(true); expect(await verifyCommercialTransportHmac('x'.repeat(32), envelope)).toBe(false); });
  it('rejects traversal-like callback endpoints and stale/oversized attempts', async () => { const unsigned = { schema: 'nexyfab.precision-cad-commercial-execution.v2' as const, job, issuedAt: '2026-08-22T00:00:00.000Z', expiresAt: '2026-08-22T01:00:00.000Z', callbackUrl: 'https://worker.example.test/../secret', leaseCapability: 'x'.repeat(43) }; const envelope = { ...unsigned, transportHmac: await hmacCommercialTransport('s'.repeat(32), unsigned) }; expect(validateCommercialTransport(envelope)).toContain('transport_expiry_invalid'); expect(validateCommercialTransport({ ...envelope, callbackUrl: 'http://127.0.0.1/callback' })).toContain('callback_url_invalid'); });
});
