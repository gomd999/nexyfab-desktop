# FEA contracts

Versioned data contracts shared by the Precision CAD application, the FEA compute container, and the platform-owned FEA worker.

This package contains transport-safe request, result, progress, persistence, and validation shapes only. Solver implementation and UI code must not be imported here.
